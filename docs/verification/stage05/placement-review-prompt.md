# Final placement and privacy follow-up review

Perform a focused read-only follow-up review. Do not modify files or run commands/tests. Read only the current implementation and its tests. Keep the response below 1,000 words; list concrete defects with severity, file/line, and a reproducible trigger. Finish with a verdict on these changed paths, distinguishing missing evidence from bugs.

The earlier Stage 05 review found a reachable deadlock when the only human hid their hand during a bot's turn. `waitingSeat` stayed null and bots were paused without a reveal control. A store test reproduced this; the implementation now preserves a manual cover/reveal seat and honors manual conceal even when automatic hotseat covers are disabled. Check `apps/web/src/store/session-store.ts`, its tests, the local game route, and `GameReadOnly.tsx` for the fix and its lifecycle. Check three-human optional/mandatory handoff behavior only insofar as this fix affects it.

The user extended explicit confirmation to all building placements. This is intentional and must not be reverted: setup and ordinary settlements, ordinary roads, free roads, and city upgrades all preview on first selection, allow changing/cancelling the candidate, and submit only through a piece-specific on-board Confirm action. Robber selection stays immediate. The UI stores one discriminated placement candidate, clears it on revision/privacy changes, and resolves the current legal command at confirmation. Check `GameActions.tsx`, `PlacementConfirmation.tsx`, `placement-confirmation-layout.ts`, the session store, and their tests.

The renderer now shows approved inset dashed road markers; white rings on dark bases for empty settlement sites; outside corner brackets and an up-chevron for eligible city upgrades. It reuses actual building color, player shape, and artwork for the preview, temporarily hides the underlying settlement during a city preview, and restores it on cancel. The DOM popover follows renderer view-change notifications and clamps inside board intersected with viewport, with enough gap for the preview. Check `packages/renderer/src/BoardRenderer.ts`, `types.ts`, `BoardView.tsx`, and focused tests for actual lifecycle or stale-preview bugs. Ignore subjective styling preferences; the coordinator is checking screenshots separately. No game rules belong in the renderer.

Check `apps/web/tests/local-game.e2e.ts` for explicit settlement/city confirmations in every driver and actual privacy-cover/hidden-hand assertions in three-human setup. A trusted golden-prefix fixture now covers a paid city at input42 with exact cost/piece transitions, plus paid settlement and free-road paths. A DEV-hook observation may briefly be absent while React reinstalls it; waiting for this read-only diagnostic is acceptable, but direct engine submissions in full-game browser drivers are not.

The broad read-only review already checked production events, save/replay validation, timers, trade/card command shapes, rematch/navigation, query/store separation, event-log privacy, port geometry, and renderer disposal without finding another blocking defect. Do not repeat that whole review. The entire local `pnpm check` passed304 tests across72 files. Final frozen-source full browser/CI acceptance still follows this review. The physical-phone and current-host simulation timing limitations remain documented; do not represent them as resolved by this code review.
